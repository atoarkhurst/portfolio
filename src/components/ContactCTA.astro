<aside>
	<div class="wrapper inner">
		<p>I’m open to what’s next</p>
		<h2>Building something<br/><em>people will use?</em></h2>
		<a href="mailto:atoparkhurst@gmail.com">Let’s talk about it <span>↗</span></a>
	</div>
</aside>
<style>
	aside { background:var(--accent); color:white; padding-block:clamp(5rem,10vw,9rem); }
	.inner { display:flex; flex-direction:column; align-items:flex-start; }
	p { text-transform:uppercase; letter-spacing:.14em; font-size:.72rem; font-weight:600; margin-bottom:2rem; }
	h2 { color:white; font-size:clamp(2.7rem,7vw,6.5rem); } em { color:var(--accent-2); font-weight:500; }
	a { margin-top:3rem; background:var(--accent-2); color:#171714; text-decoration:none; padding:1rem 1.4rem; border-radius:999rem; font-weight:600; transition:transform var(--theme-transition); }
	a:hover { transform:translateY(-3px); } a span { margin-left:1rem; }
</style>
